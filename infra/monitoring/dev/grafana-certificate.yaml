apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
    name: grafana-cert
    namespace: monitoring
spec:
    commonName: "grafana.local.investager.com"
    dnsNames:
        - "grafana.local.investager.com"
