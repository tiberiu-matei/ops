apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
    name: grafana
    namespace: monitoring
spec:
    secretName: grafana-tls
    issuerRef:
        name: letsencrypt
        kind: ClusterIssuer
