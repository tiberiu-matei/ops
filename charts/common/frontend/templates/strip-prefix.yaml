apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
    name: {{ .Values.application.name }}-strip
spec:
    stripPrefix:
        prefixes:
            - {{ .Values.application.prefix }}
